{
  "name": "team-task-manager",
  "version": "1.0.0",
  "description": "Team task manager with authentication, projects, and task tracking",
  "main": "server/server.js",
  "scripts": {
    "start": "cd server && node server.js",
    "dev": "cd server && nodemon server.js",
    "build": "cd client && npm install && npm run build",
    "postinstall": "cd client && npm install && npm run build",
    "test": "jest",
    "lint": "eslint ."
  },
  "keywords": [
    "task-manager",
    "team-collaboration",
    "nodejs",
    "express",
    "mongodb"
  ],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^9.8.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.4"
  }
}